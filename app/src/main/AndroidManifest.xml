<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.cxp.androidbase">

    <uses-permission android:name="android.permission.INTERNET"/>

    <!--获取通讯录数据-->
    <uses-permission android:name="android.permission.READ_CONTACTS"/>
    <!--获取打电话权限-->
    <uses-permission android:name="android.permission.CALL_PHONE"/>
    <!--获取发短信权限-->
    <uses-permission android:name="android.permission.SEND_SMS"/>

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".broadcast_receiver.BroadcastReceiverActivity" />
        <activity android:name=".sqlite.SQLiteActivity"/>
        <activity android:name=".content_provider.ContentProviderActivity"/>
        <activity android:name=".webview.WebViewActivity"/>
        <activity android:name=".content_provider.ReadContactsActivity"/>
        <activity android:name=".content_provider.SystemUriActivity"/>

    </application>

</manifest>